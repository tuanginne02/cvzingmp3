import classNames from 'classnames/bind';
import styles from './LoadMusic.module.scss';


const cx = classNames.bind(styles);

function Zing() {
  return (
    <div className={cx('Zing')}>
   
    </div>
  );
}

export default Zing;
